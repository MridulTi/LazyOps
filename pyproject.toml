[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lazyops"
version = "0.1.0"
dependencies = [
    "typer>=0.12.0",
]

[tool.setuptools]
packages = ["lazyops_cli"]

[project.scripts]
lazyops = "lazyops_cli.cli:run"