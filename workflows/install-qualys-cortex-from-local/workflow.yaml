id: install-qualys-cortex-from-local

name: "Install Qualys Cortex From Local"

description: "Install Qualys and Cortex from local packages via SSH. Required env: config file path as arg, AWS_REGION, SSH_KEY_DIR."

runtime: python

entrypoint: script.py

version: 1.0.0

inputs:
  - name: config
    required: true
