id: bitbucket-repo-users

name: "Bitbucket Repo Users"

description: "Audit Bitbucket repository user access. Required env: BITBUCKET_EMAIL, BITBUCKET_TOKEN."

runtime: python

entrypoint: script.py

version: 1.0.0
