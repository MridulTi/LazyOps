id: setup-redis-exporter

name: Setup Redis Exporter

description: Install redis exporter on servers from a list file.

runtime: bash

entrypoint: script.sh

version: 1.0.0

inputs:
  - name: servers-file
    required: true
