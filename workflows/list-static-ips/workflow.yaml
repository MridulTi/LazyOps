id: list-static-ips

name: "List Static IPs"

description: "Export non-ASG EC2 instances to CSV. Required env: AWS_REGION or REGION."

runtime: bash

entrypoint: script.sh

version: 1.0.0

inputs:
  - name: output-file
