id: restart-pods

name: Restart Pods

description: Restart unhealthy Kubernetes deployments

runtime: bash

entrypoint: script.sh

inputs:
  - name: namespace
    required: true

  - name: deployment
    required: true