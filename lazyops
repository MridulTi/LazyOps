#!/usr/bin/env bash
# Always run LazyOps CLI from the project virtual environment.
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=scripts/ensure_venv.sh
source "$ROOT_DIR/scripts/ensure_venv.sh"

exec "$VENV_DIR/bin/lazyops" "$@"
